---
# TODO This phase fails once

- name: Prepare IDM
  hosts:
    - idm.osgiliath.test
  tasks:
    - name: "install fedora ansible prerequisites"
      ansible.builtin.raw: dnf install -y python3 python3-libdnf5
      changed_when: false
      become: true
    - include_role:
        name: tcharl.freeipa_server
      vars:
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
    - include_role:
        name: tcharl.ansible_nameserver
      vars:
        standalone_role: False
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
- name: Prepare IPA clients
  hosts:
    - master.osgiliath.test
    - node1.osgiliath.test
  tasks:
    - name: "install fedora ansible prerequisites"
      ansible.builtin.raw: dnf install -y python3 python3-libdnf5
      changed_when: false
      become: true
    - include_role:
        name: tcharl.ansible_securehost
      vars:
        standalone_role: False
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
    - include_role:
        name: tcharl.ansible_nameserver
      vars:
        standalone_role: False
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
- name: configure containerization prereq # Execute master and nodes at the same time: one fact is created by the master and reused by nodes
  hosts: # use this order: master should be created before node
    - master.osgiliath.test
    - node1.osgiliath.test
  roles:
    - role: geerlingguy.containerd
  become: Yes

- name: configure swap # Execute master and nodes at the same time: one fact is created by the master and reused by nodes
  hosts:
    - master.osgiliath.test
    - node1.osgiliath.test
  vars:
    swap_file_state: absent
  roles:
    - role: geerlingguy.swap
  become: Yes
- name: Prepare Kubernetes master
  hosts:
    - master.osgiliath.test
  tasks:
    - include_role:
        name: tcharl.ansible_orchestration
      vars:
        standalone_role: False
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        master_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
- name: Prepare Kubernetes nodes
  hosts:
    - node1.osgiliath.test
  tasks:
    - include_role:
        name: tcharl.ansible_orchestration
      vars:
        standalone_role: False
        secure_logs: False
        skip_hostname_assert: True # Nic name is randomized at startup
        skip_nameserver_assert: True
        preferred_nic: "ens7"
        idm_preferred_nic: "ens7"
        master_preferred_nic: "ens7"
        company_domain: osgiliath.test
        company_realm_password: '123ADMin'
        company_ad_password: '123ADmPass'
